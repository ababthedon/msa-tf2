# Requirements for MSA Evaluation Tools
# Install with: pip install -r requirements_evaluation.txt

# Core ML/Data Science
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Deep Learning (TensorFlow 2.x)
tensorflow-macos>=2.13.0; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow>=2.13.0; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-metal; sys_platform == 'darwin' and platform_machine == 'arm64'

# Data I/O
h5py>=3.1.0

# Utilities
tqdm>=4.62.0

# Optional but recommended
transformers>=4.0.0

